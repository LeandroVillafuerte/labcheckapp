import { View, Text } from "react-native";
import React from "react";

const ItemRegisterList = ({ item }) => {
  console.log(item);
  return (
    <View>
      <Text>ItemRegisterList</Text>
    </View>
  );
};

export default ItemRegisterList;
